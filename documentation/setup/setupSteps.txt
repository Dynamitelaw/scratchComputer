####
# Logisim
####

Download from http://www.cburch.com/logisim/download.html
Setup as default program for .circ files

linux - "sudo apt-get install logisim"


####
# Sublime Text
####

install RISC-V syntax highlighting from https://github.com/Andreymazepas/riscv-syntax
install Verilog and System Verilog highlighting

####
# Python
####

Install python
Install required libraries:
	-pandas

####
# Verilog setup
####

https://github.com/steveicarus/iverilog
https://iverilog.fandom.com/wiki/GTKWAVE
https://www.youtube.com/watch?v=xd-ZvBJiv3M
"sudo apt-get install iverilog"
"sudo apt-get install gtkwave"


####
# Qflow
####

Optional: required for layout and static timing analysis
http://opencircuitdesign.com/qflow/
http://opencircuitdesign.com/magic/


-Installing yosys
	sudo apt-get install yosys

	git clone git@github.com:YosysHQ/yosys.git
	#go into git repo
	sudo apt-get install build-essential clang bison flex
	sudo apt-get install libreadline-dev gawk tcl-dev libffi-dev git
	sudo apt-get install graphviz xdot pkg-config python3 libboost-system-dev
	sudo apt-get install libboost-python-dev libboost-filesystem-dev zlib1g-dev
	make config-gcc
	make
	sudo make install

	make test
	#I am passing the vast majority of tests, but am failing "svinterface1". Let's hope it's not important

-Install graywolf
	sudo apt-get install graywolf

	git clone git@github.com:rubund/graywolf.git
	#go into git repo
	sudo apt-get install libgsl-dev
	sudo apt-get install libx11-dev
	sudo apt install cmake

	mkdir build
	cd build
	cmake ..
	make  
	sudo make install  

	make test  #run tests
	#I am failing 1/6 test (5:map9v3-twmc). Let's hope this is not important

-Install Magic
	sudo apt-get install magic
	
	git clone git://opencircuitdesign.com/magic
	#go into git repo
	sudo apt-get install mesa-utils
	sudo apt-get install freeglut3-dev
	sudo apt install csh
	./configure
		#Configure is warning about TCL install missing
		sudo apt-get install tcl
		#tcl is already installed. find out where it is
		#command is "tclsh" for some reason.
		whereis tclsh
		sudo cp /usr/bin/tclsh /usr/bin/tcl
		./configure --with-tcl=/usr/bin/tclsh
		#cant figure it out, so fuck it. I'll see it it matters later

	make
	#we're failing make. I suspect it is the tcl issue


-Install Qflow
	git clone git://opencircuitdesign.com/qflow
	#go into git repo
